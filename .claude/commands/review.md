Review the entire codebase in /Users/michalcapoun/tracer and fix all issues found. Go through every source file and:

1. **Unused code** — remove unused variables, imports, functions, and dead code branches.

2. **Best practices** — fix anti-patterns, simplify overly complex expressions, remove redundant logic.

3. **Naming conventions** — rename variables, functions, and constants that don't follow consistent conventions (camelCase for variables/functions, PascalCase for components/types, SCREAMING_SNAKE_CASE for constants).

4. **Language** — all code must be in English: variable names, function names, comments, type names, constants, file-level documentation. The only exception is UI-facing strings displayed to the user (text inside Vue templates, toast messages, button labels, placeholder text, error messages shown in the UI) — these must stay in Czech.

For each issue found, make the fix directly. After finishing all files, provide a summary of what was changed and why.
